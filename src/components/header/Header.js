import React from 'react';
import Navigation from "../navigation/Navigation";

export const Header = () => {
    return (
        <header className={"sms-header"}>
            <Navigation/>
        </header>
    )
};
